let dayOfWeek = ['Sunday', 'Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday'];

// Acessando tamanho do array do array
//console.log('--->', dayOfWeek.length);

/* acessando elementos e fazendo uma interação
 - vamos supor que queremos exibir todos os elementos do dayOfWeek,
   para isso precisamso percorrer oarray com um laço e exibir os elementos 

*/

for(let i = 0; i < dayOfWeek.length; i++){
    console.log(dayOfWeek[i]);
}



